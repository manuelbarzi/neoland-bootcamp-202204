/*function push(array, element) {
    //aprovechamos la longitud del array para agregar este nuevo elemento
    array[array.length] = element;

    return array.length;
}*/

//arguments: trata todos los elementos y los junta como una array

/*function push(array) {
    for (let i = 1; i < arguments.length; i++) {
        const element = arguments[i]

        array[array.length]
    }
}*/