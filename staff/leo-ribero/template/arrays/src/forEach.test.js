console.log ('TEST forEach')

const nums = [1, 2, 3]

{
    console.log('CASE 1')
    let sum = 0

    forEach(nums, function(elem)
    
    
    ){
        sum += elem
    }
        
    console.assert(result.forEach)
}