// 1. A function that receives an array which possibly has more arrays inside of itself
// 2. Create an empty array inside of the function
// 3. For each element of the array you're working on, check if the element is an array or not
    // 3.a. If not, add it to the empty array (step 2)
    // 3.b. If it is, then spread (...) its contents inside the empty array


    [1, 2, ['a', 'b']]