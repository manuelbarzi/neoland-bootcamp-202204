function fill (array, value, start=0, end=array.length){//tenemos que colocar todas en la funcion
for (let i=start; i<end;i++) //ponemos end igualado a array.length para poder ponerlo en la posición que ocupa array.length por si queremos que termine antes
array[i]=value 
}

/*como en cada ejercicio el número a cambiar será diferente o entre ellos serán diferente, 
tenemos que acordarnos que value será el cambio de ese número,
start desde donde empezará, end donde terminará (que no tiene porque sera a la longitud total)

*/
























