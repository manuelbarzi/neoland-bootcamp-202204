console.log('TEST splice')

describe('should insert a Feb between Jan and March', () => {
    
        console.log('CASE 1')
    
        const months = ['Jan', 'March', 'April', 'June'];
    
        const deleteElement = months.splite(1, 0, 'Feb')
    
        splice(months, deleteCount)
    
        // console.log(months, 444)

})

