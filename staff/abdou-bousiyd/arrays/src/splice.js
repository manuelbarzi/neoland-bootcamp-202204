
function splice(array, index, deleteCount, newElement) {

    const deleteCount = []

    for(let i = deleteCount; i < array.length ; i++) {

        const result = `${array[deleteCount]},${array[i]}`.split(',') 

        


        // console.log(result  , 55)
        // return array[deleteCount]



    }



    // console.log(array)

}