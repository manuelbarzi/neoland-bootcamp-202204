describe ('Fakay.prototype.filter', function() {
    
    it ('should copy elements with start + end index', function() {
        const numbers = new Fakay ('spray', 'limit', 'elite', 'exuberant', 'destruction', 'present')
        // expected = ["exuberant", "destruction", "present"]

        expect().toBe("exuberant")
        expect().toBe("destruction")
        expect().toBe("present")
        expect(.length).toBe(3)
       
    })

    it ('NO mach result return empy Fakay', function() {
        
       
    })
})    