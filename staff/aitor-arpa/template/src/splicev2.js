
















function splice(array,str = 0, del = 0, element) {
    debugger
    for (let i = 0; i < array.length; i++) {
        if (array.length === del)
        array[length + 1] = element
        if (i === str)
        array[i] = element
        if (i === up)
        array[+1]

       
    }
     return array
}

 