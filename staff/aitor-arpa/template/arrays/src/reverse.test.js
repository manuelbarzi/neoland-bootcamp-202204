// invierte el orden de los elementos del array
// in place es que devuelve el mismo array

/* 
{ console.logo ('Test Reverse')

const numeros = [1,2,3,4]
    {
        console.log ('Caso 1')
        reverse(numeros)


        console.assert (array[0] === 4)
        console.assert (array[1] === 3)
        console.assert (array[2] === 2)
        console.assert (array[3] === 1)
        console.assert (array.lentgh === 4)


    }
}  */


describe('',()=> {

    test('', () => {



        
    })
})