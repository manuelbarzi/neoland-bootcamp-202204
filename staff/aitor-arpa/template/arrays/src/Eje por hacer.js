// TODO acabar unshif shif indexOf e reverse Resolver Grupby, keys , 

// Practicar el reduce


const arr1 = [0, 1, 2, [3, 4]];

console.log(arr1.flat());
// expected output: [0, 1, 2, 3, 4]

const arr2 = [0, 1, 2, [[[3, 4]]]];

console.log(arr2.flat(2));
// expected output: [0, 1, 2, [3, 4]]
/* 


describe('Every', function(){
   

    test('Macht the element',function(){
        const array = [ 'pc','pc','pc']
        result = every(array)
        expect(result).toBe(true)
    })

    test('No macht the element', function(){

        const array = [ 'pc','cp','pc']
        result = every(array)

        expect(result).toBe(false)
    })

})
 */
{
